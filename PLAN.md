# 数値計算プラン — 120 GPa における ODMR 感度の青色励起波長最適化とレーザーパワー依存性

**問い:** 120 GPa の超高圧下で ODMR 測定の感度を最大化する青色レーザー波長は何 nm か。
また、その最適波長はレーザーパワーによって変わりうるか。

**出発点:** 既存コード(`code/nv_model.py`, `code/fig2_blue_wavelength_sweep.py`)。
ロックイン感度 η ∝ Δν/(C√R) を、Franck–Condon 吸収エンベロープ σ_abs(λ,P) と
定常 NV⁻ 分率 f₋ から計算するフェノメノロジカルモデル
(物理アンカーは Ho, Dailledouze, Žalandauskas *et al.*, arXiv:2606.02399)。
現状の答え: **120 GPa で λ_opt ≈ 475 nm**(固定パワー条件)。

**現行モデルの限界(問2 に効く):** `f_minus()` ではイオン化レート G_ion と
再結合レート G_rec がどちらも励起強度 I に**線形**(定数項は rbg のみ)。
このため η(λ) の形は絶対パワーにほぼ非感応で、「パワーで最適波長が変わるか」は
**現行モデルでは原理的に答えられない**。物理的には ³E 経由のイオン化は
2 光子過程(低強度で ∝ I²)であり、光学遷移の飽和・ODMR 線幅のパワー広がりも
入っていない。よって Part B のモデル拡張が本計画の中核となる。

---

## Part A — 問1: 120 GPa での最適波長の確定(既存モデルの精密化)

**A1. 基準スイープの精密化**
λ = 402–560 nm を 0.1 nm 刻みで η(λ; 120 GPa) を計算し、argmin 近傍を
放物線補間してサブ nm 精度で λ_opt を決める(現状は 900 点グリッドの argmin)。

**A2. λ_opt の不確かさ帯**
`mc_band` を流用し、n ≥ 500 のモンテカルロで **λ_opt そのものの分布**
(16–84% 区間)を出す。現状の帯は η(λ) 曲線に対するものであり、
答えである「最適波長 ± 何 nm」を直接出すのがここでの追加点。

**A3. アンカー感度(トルネード解析)**
文献アンカー {ZPL シフト (Emax, P0), S_abs 勾配, IP(³A₂), 線幅モデル, w0} を
文献誤差の範囲で 1 つずつ ± に振り、∂λ_opt/∂(パラメータ) を表にする。
「475 nm という答えがどのアンカーに支配されているか」を特定し、
校正(Part C)で優先的に締めるべき量を決める。

**A4. 実用トレランス帯**
η(λ) ≤ 1.05·η_opt(および 1.10)を満たす波長区間 [λ_lo, λ_hi] を計算し、
入手性の高いレーザー線(450, 457, 473, 488, 493, 505 nm)がこの帯に
入るかを判定する。答えを「475 nm、ただし ±Δλ なら実質同等(例: 473 nm 市販線で可)」
という実験装置選定に使える形にする。

**A5. 回帰チェック**
100 GPa で λ_opt = 487 nm、green/blue クロスオーバー ≈ 86 GPa、
η(457)/η_opt(120 GPa) ≈ 1.12 が再現されることを自動テスト化する。

---

## Part B — 問2: パワー依存性(モデル拡張 + 2D 最適化)

### B1. 強度陽依存モデルへの拡張(`nv_model_power.py`)

無次元強度 u = I/I_ref(I_ref は校正点の強度)を導入し、レート方程式を書き換える:

- **³E 励起と飽和:** Γ_exc = k·σ_abs(λ,P)·u、³E 占有率
  n_E = Γ_exc/(Γ_exc + Γ_decay)。低強度で ∝ u、高強度で飽和。
- **2 光子イオン化(現行の線形項 a_es·σ·I を置換):**
  G_ion,ES = a_es′·u·n_E → 低強度で ∝ u²、飽和域で ∝ u。
  120 GPa では IP(³E) = 1.63 eV(≈760 nm)なので青色域では常に開いている。
- **1 光子基底状態イオン化:** G_ion,GS = a_gs·ReLU(Eγ − IP(³A₂))·u(現行同様)。
- **再結合:** NV⁰ の励起を経る過程として G_rec = r0·σ_NV⁰(λ,P)·u·n_E⁰ + rbg。
  現行は σ_NV⁰ を σ_abs(NV⁻)で代用しているため、NV⁰ 用の FC エンベロープ
  (ZPL 2.156 eV + 圧力シフト)を別途構成する。rbg(暗再結合)のみ u 非依存。

これで f₋ = G_rec/(G_rec + G_ion) が **f₋(λ, u)** になる。

### B2. 光子レートの飽和

R = f₋ · β·Γ_exc/(1 + Γ_exc/Γ_sat)。高パワーで R が頭打ちになり、
√R によるショットノイズ利得が消える一方でイオン化は増え続ける、
という競合を入れる。

### B3. コントラストと線幅のパワー依存(CW-ODMR 標準理論)

Dréau *et al.*, PRB **84**, 195204 (2011) 型の閉形式を使う:
- コントラスト C(λ, u, Ω_R): 光ポンピングレート Γ_p ∝ f₋·σ_abs·u と
  MW ラビ周波数 Ω_R の競合。
- 線幅の光パワー広がり: Δν(u) = Δν_P·√(1 + Γ_p/Γ_c)(Δν_P は圧力歪み項)。

これにより η(λ, u) = Δν(u) / [C(λ,u)·√(R(λ,u))] が完全に u 依存になる。

### B4. 数値実験

1. **2D マップ:** η(λ, u) を λ = 402–560 nm(0.2 nm)× u = logspace(−2, +2, 61)
   @120 GPa で計算し、各 u での argmin から**稜線 λ_opt(u)** を抽出する。
2. **判定基準:** λ_opt(u) の運用パワー範囲での変動幅を A4 のトレランス帯と比較。
   - 変動がトレランス内 → 「パワーによらず 475 nm 帯で固定してよい」
   - 超える → 低パワー/高パワーそれぞれの推奨波長を提示。
3. **物理的予想(計算で定量化する仮説):** u が大きいと 2 光子イオン化
   (低強度極限で ∝ σ_abs²u²)が σ_abs ピーク直上の励起を罰するため、
   f₋ 維持の観点から λ_opt がサイドバンド端寄り(赤側)へずれる。
   逆に低パワーでは rbg が f₋ を支えるので σ ピーク直上(475 nm)が最適のまま。
   各項(2 光子イオン化 / R 飽和 / 線幅広がり)を個別に on/off して
   λ_opt シフトへの**寄与分解**を行う。
4. **大域最適点:** (λ*, u*) = argmin η。DAC の加熱上限・集光限界などの
   実験制約線を η マップに重ね、制約下での運用点を提示する。
5. **実在レーザー線の比較:** 457 / 473 / 475 / 488 nm について η(u) 曲線を
   重ね描きし、「このパワーならこの線」という運用表を作る。

### B5. 不確かさ帯

拡張で増えた knobs(a_es′, Γ_sat, σ_NV⁰ 比, Γ_p 係数, Γ_c)を
`default_randomiser` に追加し、λ_opt(u) 稜線の 16–84% MC 帯を付ける。

### B6. 熱効果(オプション)

吸収発熱 ΔT ∝ u による ZPL シフト・線幅増をフェノメノロジカルに入れ、
λ_opt(u) への摂動として効くかを一次評価する(効くようなら本組み込み)。

---

## Part C — 校正計画(フェノメノロジカル定数を実験で固定する)

**C1.** 予定されている (I₄₀₅, I₄₅₇) 2 ビーム強度掃引データに対して
{a_gs, a_es′, r0, rbg, Γ_sat, w0} を最小二乗 + MCMC(または bootstrap)で
フィットする。観測量: PL レート、ODMR コントラスト、可能なら
NV⁻/NV⁰ スペクトル比。

**C2. 識別可能性の事前チェック:** フィッシャー情報 / プロファイル尤度で、
予定の掃引だけで a_es′(2 光子係数)と Γ_sat(飽和)を分離できるかを
合成データで確認する。分離できない場合は追加測定
(例: 457 nm と 488 nm それぞれの単色パワー掃引)を提案する。

**C3.** フィット事後分布を B5 の MC 帯に伝播し、最終的な
λ_opt(u) ± 帯を「実験で校正済みの答え」として出す。

---

## Part D — 実装と成果物

| 成果物 | 内容 |
|---|---|
| `code/nv_model_power.py` | NVModel を拡張した強度陽依存モデル(B1–B3) |
| `code/fig3_power_sweep.py` | (a) η(λ,u) ヒートマップ+稜線, (b) λ_opt vs u + MC 帯, (c) 候補レーザー線の η(u) |
| `code/fig4_tornado.py` | A3 のアンカー感度トルネード図 |
| `code/tests/` | 低強度極限で現行モデルと一致すること、A5 の回帰値 |
| 校正ノートブック | C1–C3 のフィットと事後伝播 |

**数値設定の目安:** λ グリッド 0.1–0.2 nm、u は 4 桁を 61 点 log 刻み、
MC n = 250–500、argmin は放物線補間で精密化。全計算はラップトップで数分程度。

---

## 期待される答えの形

- **問1:** λ_opt = 475 nm(MC 16–84%: ±X nm)。η 5% 以内の実用帯 [λ_lo, λ_hi] と、
  市販線(473 nm / 488 nm)の可否判定。
- **問2:** λ_opt(u) 曲線と寄与分解。「低パワー極限では 475 nm、
  パワーを上げると 2 光子イオン化により最適波長が Y nm/decade で赤側へずれる
  (またはトレランス内で不変)」という定量的な結論、および推奨運用点 (λ*, u*)。
