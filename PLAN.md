# 数値計算プラン v2 — 120 GPa における ODMR 感度最適化

改訂: 2026-08-27(v1 は 2026 年前半、v3 凍結前に書かれたもの)

> **改訂の理由**
> v1 は `nv_model.py`(現象論的 Franck–Condon エンベロープ)を出発点に
> 「120 GPa で λ_opt ≈ 475 nm」を現状の答えとして立て、Part A で
> それを精密化し、Part B でパワー依存性を調べる計画だった。
>
> その後 v3 が Ho の公表スペクトルを外部カーネルとして凍結し、
> Addendum A1–A3 と正誤表 E1–E3 が実行された。結果として
> **v1 の前提のうち3つが成立しない**ことが分かったので、
> ここでは実行済み事項を記録し、残る計算だけを再定義する。
>
> 実行結果: `docs/theory_a1_numerical_execution.md`、
> `docs/theory_a2_multiplicity.md`、`docs/theory_a3_branch_exchange.md`、
> `docs/novelty_and_exponent_audit.md`

---

## 0. v1 の前提のうち成立しなかったもの

### 0.1 「現状の答えは 475 nm」— カーネル依存の記述だった

475.5 nm は **v1 の FC エンベロープの出力**であり、v3 が凍結した
Ho カーネルの出力(**440.65 nm**)ではない。両者は 120 GPa で **34.87 nm** 違い、
5% 許容帯の半幅の2倍を超える。v3 カーネルでは 473 nm の損失は
0.2% ではなく **20.5%** である。

したがって v1 の Part A(475 nm の精密化)と A3(「475 nm がどのアンカーに
支配されているか」)は、**問いの立て方ごと無効**になった。

### 0.2 「現行モデルでは原理的に答えられない」— 答えられた

v1 は「`f_minus()` でイオン化も再結合も強度に線形なので、パワーで最適波長が
変わるかは原理的に答えられない」とし、Part B のモデル拡張を中核に据えた。

A2 の**定理 G** がこれを構造的に解決した。応答は単一のスカラー
\(E=2c+s+2w\) を通じてしか \(\eta\) に入らず、分岐は \(En>1\) と同値である。
そして Dréau の公表フィットが **E = 3、En = 6、\(\rho^*=1/5\)** を与える。
**モデル拡張を待たずに、分岐が起こることは既に確定していた。**

さらに定理 G の系により、**機構の同定は \(\eta\) からは原理的に不可能**である。
v1 の B4.3「各項を on/off して λ_opt シフトへの寄与分解を行う」は
**実行しても意味を持たない**(飽和+線幅広がりとコントラスト低下は
\(\eta\) 曲面が厳密に一致する)。

### 0.3 「最適波長が動く」— 動くのではなく割れる

v1 の B4 は \(\lambda_{\rm opt}(u)\) を「稜線」として抽出する計画だった。
A2 の**定理 M** により、最適点は点ではなく**準位集合**であり、
パワーを上げると**多重度が跳ぶ**。稜線という描像そのものが成り立たない。

v1 の物理的予想(「2光子イオン化が σ ピーク直上を罰するので λ_opt が
赤側へずれる」)も、定理 M(b) により電荷変換は (M) 媒介チャネルであり
**単独では最適点を割れない**(A1 の P7)。

### 0.4 A5 の回帰値について

v1 は「100 GPa で λ_opt = 487 nm、green/blue クロスオーバー ≈ 86 GPa、
η(457)/η_opt(120 GPa) ≈ 1.12」を回帰テスト化するとした。
これらは **v1 モデルの内部整合性テストとしては有効**で、
`tests/test_freeze.py` に実装済みである。

ただし **86 GPa を分岐交代(A3)と同一視してはならない**。
v1 のエンベロープは全圧力で単峰なので交代する分岐を持たず、
86 GPa は単峰ピークが 457/532 の中点(494.5 nm)を通過する幾何にすぎない。
同一カーネル内で計算すると固定波長交差は 51.4 GPa に出る
(`docs/novelty_and_exponent_audit.md` §4)。

---

## 1. 実行済み(v1 の Part A/B に相当する部分)

| v1 の項目 | 状態 | 実際に得られたもの |
|---|---|---|
| A1 サブ nm 精度の λ_opt | 済(カーネル差し替え) | Ho カーネルで **440.65 nm** |
| A2 λ_opt の不確かさ帯 | **保留** | 下記 §2.1 |
| A3 アンカー感度 | 無効化 | 問いが 475 nm 前提だった |
| A4 実用トレランス帯 | 済 | 5% 帯 **[426.43, 457.90] nm**(非対称) |
| A5 回帰チェック | 済 | `tests/test_freeze.py`(v1 の内部整合として) |
| B1–B3 強度陽依存モデル | 不要に | 定理 G が \(E\) に集約、Dréau が \(E=3\) を供給 |
| B4 2D マップ・稜線 | 再定義 | 稜線ではなく**多重度梯子** \(N(I)\) |
| B4.3 寄与分解 | 無効化 | 定理 G により \(\eta\) からは同定不能 |
| B5 不確かさ帯 | **保留** | 下記 §2.1 |
| B6 熱効果 | 未着手 | 下記 §2.3 |
| C1–C3 校正 | 実験待ち | `docs/next_step_power_dependence_experiment.md` |

**追加で得られたもの(v1 に無かった):**

- 定理 M(多重度梯子)と較正不要の段位置予言 \(I_k/I_c=A_{\max}/A_k\)
- 定理 G(ゲージ縮退)と機構同定不能性
- 定理 X(圧力による分岐交代)と帯域幅依存の \(P^*\)
- 事前登録検定 T1、T4–T11
- 原図照合(E3): サイドバンドは 1% 以内で有効、ZPL ピーク高はクリッピングで無効

---

## 2. 残っている計算

### 2.1 不確かさ伝播(v1 の A2 / B5 の後継)— 優先度 中

v1 は `mc_band` で \(\lambda_{\rm opt}\) の分布を出す計画だった。
枠組みが変わったので、伝播すべき対象も変わる。

**伝播すべき量:**

| 量 | 不確かさ源 |
|---|---|
| 5% 許容帯の端 [426.43, 457.90] nm | カーネル抽出誤差(サイドバンドは 1% 以内と判明) |
| 梯子の段位置 \(I_k/I_c=1/a_k\) | 臨界値 \(a_k\) の抽出誤差 |
| \(\rho^*=1/(En-1)\) | Dréau の \(\Gamma_p^\infty,\Gamma_c^\infty\) のフィット誤差 |
| \(r(P)\) と臨界帯域 \(1/r(P)\) | DWF と \(\sigma_{\rm SB}\) の抽出誤差 |

**やり方:** サイドバンド側は原図照合で 1% 以内と分かったので、
モンテカルロの randomiser はその実測誤差に置き換える。
ZPL 側は panel (c) の DWF 読み取り誤差(目盛較正から ~2%)を使う。
**v1 の `default_randomiser` の knobs は使わない** — 対応する量が消えたため。

### 2.2 段幅と分解能の設計計算 — 優先度 高

A2 の段幅は ×1.05, ×1.23, ×1.93, ×1.19、そして \(N=6\) は ×1.003 で到達不能。
**実験のパワー刻みを決めるには、測定誤差を入れた段の検出力計算が要る。**

- 各段について、\(N\) の変化を検出するのに必要な \(\eta\) の測定精度
- 対数等間隔の点数と、段を見落とす確率
- \(N=6\) 以外に到達不能な段が無いことの確認

これは実験計画に直結するので、**次に着手すべき計算**である。

### 2.3 熱効果(v1 の B6)— 優先度 低、ただし位置づけが変わった

v1 は吸収発熱による ZPL シフト・線幅増を \(\lambda_{\rm opt}(u)\) への摂動として
評価する計画だった。A3 が出たことで**温度の役割が明確になった**:

温度は \(S\) をほとんど変えず \(\Gamma\) だけを変える。したがって
定理 X の駆動1(\(\mathrm{d}S/\mathrm{d}P>0\))には効かず、駆動2(線幅比)には効く。
**発熱は分岐交代を起こさないが、\(P^*\) を動かしうる。**
E1 の hot-band 議論(532 nm)とも接続する。

### 2.4 常圧での梯子予言 — 優先度 中(投稿戦略に依存)

Dréau の \(E=3,n=2,\rho^*=1/5\) と常圧 NV の吸収スペクトルを組み合わせれば、
**DAC を持たない研究室が検定できる予言**が出る。
ただし A3 を主に据えるなら圧力が本質なので、この道は取らない
(`docs/positioning_in_high_pressure_sensing.md` §4)。
投稿先の決定後に判断する。

---

## 3. 成果物(v1 の Part D の後継)

| 成果物 | 状態 |
|---|---|
| `code/ho_spectrum_model.py`, `ho_odmr_sensitivity.py` | 済(v3 凍結カーネル) |
| `code/theory_a1_generalization.py` + 図 | 済 |
| `code/theory_a2_multiplicity.py` + 図 | 済 |
| `code/theory_a3_branch_exchange.py` + 図 | 済(E3 により ZPL 側は要注意) |
| `code/figure_validation.py` | 済(原図照合) |
| `code/dreau_exponent.py` | 済(\(E\) の文献接地) |
| `code/nv_model_power.py`, `fig3_power_sweep.py` | **不要**(§0.2) |
| `code/fig4_tornado.py` | **不要**(§0.1、問いが無効) |
| 段の検出力計算 | **未着手**(§2.2) |
| 不確かさ伝播 | **未着手**(§2.1) |
| 校正ノートブック | 実験待ち |

---

## 4. 現時点の答え

**問1(最適波長):** 120 GPa の光学限界で **440.65 nm**、5% 帯 **[426.43, 457.90] nm**。
457 nm はペナルティ ×1.0449 で帯の内側。473/488 nm は帯の外。
ただしこれは**非光学因子が波長非依存という仮定の下**であり、
その仮定の成否が測定対象である(A1 の P1)。

**問2(パワー依存性):** 「最適波長が動くか」ではなく
「**最適点の個数が跳ぶか**」が正しい問いだった(定理 M)。
分岐条件 \(En>1\) は Dréau の公表フィットで \(En=6\) と満たされており、
梯子の最下段は飽和パワーの **10.4%** に位置する。
段位置は低パワースキャンから**較正なしに予言できる**。

**問3(v1 に無かった問い、圧力):** 大域最適を担う分岐が圧力で交代する(定理 X)。
駆動は公表パネルで確認済み(\(S_{\rm abs}\) +51%、DWF ×9.07 減)。
\(P^*\) は励起帯域幅の関数で、\(W\approx1.5\)–9.7 meV のとき 0–120 GPa 内に落ちる。
